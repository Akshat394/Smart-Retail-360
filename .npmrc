# Suppress non-critical audit warnings
audit-level=high

# Use legacy peer deps for compatibility
legacy-peer-deps=true

# Suppress funding messages
fund=false

# Suppress update notifications
update-notifier=false 